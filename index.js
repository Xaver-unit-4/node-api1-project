const server = require('./api/server');

const port = 5000;

server.listen(port, () => {
  console.log('This message displays in the terminal when the server is started on port 5000')
})

// START YOUR SERVER HERE
